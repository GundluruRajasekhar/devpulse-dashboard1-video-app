# devpulse-dashboard1-video-app
Here's a narrated walkthrough page — open it and click "▶ Play narration", and it reads a 10-section explanation of DevPulse out loud using your browser's built-in voice (no audio file, no recording needed on my end).  How it works:  Each section highlights and auto-scrolls as it's spoken (intro → homepage → nav → profile → projects/tasks → search/
